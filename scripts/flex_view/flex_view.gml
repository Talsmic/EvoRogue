///flex_view(view);
/// @arg view
//Flex views check the current room and adjust the view based on 
//the availible space if the room allows for it

var view = argument0;

//Default Settings
var fixed_x = -1;
var fixed_y = -1;
var flexible_view = true;

//Room Specific Settings
switch ( room ) {
	
	case rm_debugmenu:
		fixed_x = 500; 
		fixed_y = 500;
		break;
	
	};
	
if ( flexible_view ) {
	
	};
else {
	
	};